# Runtime dependencies - required for ETL pipeline execution
# These versions must be tested via test_integration.py before bumping.
# Bumping any version requires running the full integration test suite to ensure
# ShelterLuv API compatibility and schema validation still work.

# Google Cloud Functions framework (for Cloud Run deployment)
functions-framework>=3.5.0,<4.0.0

# Firebase/Firestore client (for database operations)
firebase-admin>=6.5.0,<7.0.0

# HTTP client (for ShelterLuv REST API calls)
requests>=2.31.0,<3.0.0

# Browser automation (for ShelterLuv web scraping)
playwright>=1.40.0,<2.0.0

# Secret management (for credential access)
google-cloud-secret-manager>=2.20.0,<3.0.0

# JSON schema validation (for data integrity checks)
jsonschema>=4.19.0,<5.0.0
